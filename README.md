# task1-AI
Task One – MNIST Dataset – To be able to download and load any dataset
(a) Download MNIST Dataset

(b) Write a program to distinguish digits 0 – 9 using MNIST data set 

# MNIST Digit Classification

## Task (a): Download MNIST Dataset
**File:** `task_a_download_mnist.py`

Downloads and loads MNIST dataset (60k training, 10k test images).

**Run:** `python task_a_download_mnist.py`

## Task (b): Classify Digits 0-9
**File:** `task_b_classify_digits.py`

CNN model to recognize handwritten digits.

**Run:** `python task_b_classify_digits.py`

## Setup
```bash
pip install tensorflow matplotlib numpy scikit-learn seaborn
